[preview]
wrap = "yes"
max_width  = 3840    # ≥ pane width, so it's never the limit
max_height = 2160
image_filter = "lanczos3"

[mgr]
ratio = [0, 1, 1]   # hide parent, give preview 2/3 of the screen
